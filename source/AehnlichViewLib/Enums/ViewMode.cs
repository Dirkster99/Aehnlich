namespace AehnlichViewLib.Enums
{
	/// <summary>
	/// Determine whether (text) content is displayed for comparison or editing.
	/// </summary>
	public enum DisplayMode
	{
		/// <summary>Comparing (text) content (with highlighting differences in left and right view).</summary>
		 Comparing

		/// <summary>Editing (text) content (without highlighting differences in left and right view).</summary>
		, Editing
	}
}
