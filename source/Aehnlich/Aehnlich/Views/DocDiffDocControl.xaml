<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    
                    xmlns:local="clr-namespace:Aehnlich.Views"
                    xmlns:views="clr-namespace:AehnlichViewModelsLib.Views;assembly=AehnlichViewModelsLib"
                    >

    <Style TargetType="{x:Type local:DocDiffDocControl}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:DocDiffDocControl}">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                        
                        <views:FileDiffView DataContext="{Binding DocDiffDoc}" Margin="3"/>

                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>