<UserControl x:Class="AehnlichViewModelsLib.Views.OptionsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 

             xmlns:icon_src="clr-namespace:AehnlichViewLib.Themes;assembly=AehnlichViewLib"
             
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <GroupBox Header="Compare" Grid.Column="0" Margin="12" IsEnabled="False">
                <StackPanel Orientation="Vertical">
                    <RadioButton Content="Auto" IsChecked="True" Margin="3" />
                    <RadioButton Content="Text" Margin="3" />
                    <RadioButton Content="XML"  Margin="3" />
                </StackPanel>
            </GroupBox>

            <GroupBox Header="Options" Grid.Column="1" Margin="12" IsEnabled="False" >
                <StackPanel Orientation="Vertical">
                    <CheckBox Content="Ingnore case in text" IsChecked="False" Margin="3" />
                    <CheckBox Content="Ignore leading and trailing whitespaces in text" IsChecked="True" Margin="3" />
                    <CheckBox Content="Ignore insignificant whitespace nodes in XML" IsChecked="True" Margin="3" />
                </StackPanel>
            </GroupBox>

            <StackPanel Grid.Column="2" Orientation="Vertical"
                        HorizontalAlignment="Right" Margin="12">
                <Button Content="Swap" Margin="3" IsEnabled="False" />
                <Button Content="OK" Margin="3" IsEnabled="False" />
                <Button Content="Cancel"
                        Command="{Binding CloseDialogCommand}"
                        Margin="3" />
            </StackPanel>
        </Grid>
        
        <Button Grid.Column="1"
                ToolTip="Close"
                Command="{Binding CloseDialogCommand}"
                BorderThickness="0"
                BorderBrush="Transparent"
                HorizontalAlignment="Right" VerticalAlignment="Top"
                MinHeight="12" MinWidth="12"
                
                Width="{Binding Path=ActualHeight, RelativeSource={RelativeSource Self}}"
                Margin="6,0,3,3"
                >
            <Button.Background>
                <ImageBrush ImageSource="{DynamicResource {x:Static icon_src:ResourceKeys.ICON_CloseKey}}"/>
            </Button.Background>
        </Button>
    </Grid>
</UserControl>
